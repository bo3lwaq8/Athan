# Python
__pycache__/
*.pyc

# PyInstaller build output (the exe is published via GitHub Releases, not git)
build/
dist/
*.manifest

# Local runtime settings written next to the exe
# (config.json here is the shipped DEFAULT; real user settings live by the exe)
